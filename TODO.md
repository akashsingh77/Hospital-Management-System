# TODO - Fix "Failed to fetch" login

- [ ] Enable CORS in backend for `/api/auth/**` (and generally allow frontend origin).
- [ ] Verify backend is reachable from browser: `http://localhost:8081/api/auth/login`.
- [ ] If backend is reachable but still failing, confirm CORS error in browser console.
- [ ] Optionally move API base URL to env var (so frontend port matches backend).

