server:
  port: 8081

spring:
  datasource:
    url: jdbc:mysql://localhost:3306/hospital_management_system?useSSL=false&allowPublicKeyRetrieval=true&serverTimezone=UTC
    username: root
    password: akash@10

  jpa:
    hibernate:
      ddl-auto: none
    properties:
      hibernate:
        dialect: org.hibernate.dialect.MySQL8Dialect
    open-in-view: false

security:
  jwt:
    # For development only. Replace with an env var/secret in production.
    secret: change-me-please-change-me-please
    expirationSeconds: 86400


