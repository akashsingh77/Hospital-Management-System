# Hospital Management - Spring Boot Backend

This backend will provide:
- Patient CRUD (Admin only for create/update/delete)
- Patient retrieval for authorized users

## Next steps
- Ensure MySQL is running and create database `hospital_management_system`
- Update `application.yml` with your MySQL credentials
- Start the app and verify endpoints under `/api/patients`

